import React from 'react';

const BoardDetail = ({item, deleteBoard}) => {

    const deleteEventHandler = () => {
        deleteBoard(item);
    }
    return (
        <div>
            rmfo
        </div>
    );
};

export default BoardDetail;